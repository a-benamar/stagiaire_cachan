{% extends 'base.html.twig' %}

{% block title %}Matiere{% endblock %}

{% block body %}
    <h1>Matiere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idmatiere</th>
                <td>{{ matiere.idmatiere }}</td>
            </tr>
            <tr>
                <th>Nommatiere</th>
                <td>{{ matiere.nommatiere }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_matiere_index') }}">back to list</a>

    <a href="{{ path('app_matiere_edit', {'idmatiere': matiere.idmatiere}) }}">edit</a>

    {{ include('matiere/_delete_form.html.twig') }}
{% endblock %}
