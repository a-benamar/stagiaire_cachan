{% extends 'base.html.twig' %}

{% block title %}Edit Stagiaire{% endblock %}

{% block body %}
<div class="container">
    <h3 class="my-4">Modifier Stagiaire</h3>

<div class="m-3">
    {{ include('stagiaire/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" href="{{ path('app_stagiaire_index') }}">Back</a>

    {{ include('stagiaire/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
